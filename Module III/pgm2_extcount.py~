import sys
import os

fn=sys.argv[1]
def extcount(fn):
	count = 0
	xs=os.listdir(fn)
	l = []
	for i in xs:
		d=i.split('.')
		l.append(d[1])
#def word_frequency(words):
	frequency = {}
	for w in l:
		frequency[w]=frequency.get(w,0)+1
	
	for key,value in frequency.items():

		print value,key
extcount(fn)
